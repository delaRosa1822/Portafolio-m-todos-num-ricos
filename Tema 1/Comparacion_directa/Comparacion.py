# Realizamos la resta
resultado = 1.1 - 1.0

# Mostramos el resultado real guardado en memoria
print(f"Resultado de la resta: {resultado}")

# Comparamos directamente con ==
if resultado == 0.1:
    print("Es exactamente 0.1")
else:
    print("No es 0.1!")
